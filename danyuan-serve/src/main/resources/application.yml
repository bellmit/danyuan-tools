server:
  port: 8002
  servlet:    context-path: /server
  

spring:
  application:    name: danyuan-serve
  banner:    charset: utf-8
  profiles:
    active:
    - nacos
    - h2  
    - hikari

app:
  name: ${spring.application.name}
  version: @project.version@      