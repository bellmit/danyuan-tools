server:
  port: 80  servlet:    context-path: /
spring:
  application:    name: danyuan-gateway
  banner:    charset: utf-8
  profiles:
    active:
    - gateway
    - nacos
    - app


app:
  name: ${spring.application.name}
  version: @project.version@        