server:
  port: 8005
  servlet:    context-path: /database
  

spring:
  application:    name: danyuan-serve-orcl
  banner:    charset: utf-8
  profiles:
    active:
    - nacos
    - oracle
    - hikari

app:
  name: ${spring.application.name}
  version: @project.version@